tooltip for slidersreduce transform calls

var getStackTrace = function() {
    var obj = {};
    Error.captureStackTrace(obj, getStackTrace);
    return obj.stack;
  };


 1. face are removed when parameter change
 2. faces should updated in parallel with points,synchronously.

         